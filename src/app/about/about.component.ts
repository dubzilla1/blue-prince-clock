import { Component } from '@angular/core';

@Component({
  selector: 'app-about',
  template: `
  <section>
    <h2>About</h2>
    <p>Blue Prince Timer — an Angular starter app.</p>
  </section>
  `
})
export class AboutComponent { }
